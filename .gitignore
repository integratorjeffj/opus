__pycache__/
*.pyc
.DS_Store

# Working output folder at the repo root. The committed demo output under
# samples/licensed/ is deliberately tracked, so this is anchored with a
# leading slash rather than matching "licensed/" at any depth.
/licensed/
